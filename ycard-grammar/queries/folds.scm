; Folding rules for yCard

; Fold block mappings
(block_mapping) @fold

; Fold block sequences  
(block_sequence) @fold

; Fold flow mappings
(flow_mapping) @fold

; Fold flow sequences
(flow_sequence) @fold